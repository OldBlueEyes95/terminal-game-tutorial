#include <cstdint>

#include "game.h"



int main(int argc, char **argv){
    // parse 
    // quiz q;
    // quiz();
    
    int breeding = do_my_secret(); 

    if(breeding == 0)
        decrypt();

    return 0;
}
